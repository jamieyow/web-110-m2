This is the HTML project for the lab #2 is only HTML code so far so it does not include any style,
that will be add later once we start working with css the project is about cards with different prices,
and options depending on the user needs.
